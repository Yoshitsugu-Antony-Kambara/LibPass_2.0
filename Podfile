# Uncomment the next line to define a global platform for your project
platform :ios, '12.4'

target 'LibPass_2.0' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for LibPass_2.0
pod 'Alamofire'
pod 'SwiftyJSON'
  target 'LibPass_2.0Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'LibPass_2.0UITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
